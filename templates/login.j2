{% extends "layout.j2" %}
{% block title %}
Login
{% endblock title %}

{% block content %}
<div class="auth-page">
    <div class="container page">
        <div class="row">
            <div class="col-md-6 offset-md-3 col-xs-12">
                <h1 class="text-xs-center">Sign in</h1>

                {% if error %}
                <ul class="error-messages">
                    <li>{{error}}</li>
                </ul>
                {% endif %}

                <form action="{{routes['login']}}" method="post" name="form_data">
                    <fieldset class="form-group">
                        <input name="username" class="form-control form-control-lg" type="text"
                            placeholder="Your Username" />
                    </fieldset>
                    <fieldset class="form-group">
                        <input name="password" class="form-control form-control-lg" type="password"
                            placeholder="Password" />
                    </fieldset>
                    <input type="submit" class="btn btn-lg btn-primary pull-xs-right" value="Sign in" />
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock content %}